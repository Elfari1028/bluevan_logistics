server:
  port: 80


spring:
  jpa:
    hibernate:
      ddl-auto: update
  #  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://${MYSQL_ADDRESS}/${MYSQL_DATABASE:springboot_demo}
#    username: ${MYSQL_USERNAME}
#    password: ${MySQL_PASSWORD}
  jackson:
    time-zone: Asia/Shanghai
  datasource:
    hikari:
      maxLifetime:18000
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://sh-cynosdbmysql-grp-rr5vyhp0.sql.tencentcdb.com:22489/baraka_logistics&serverTimezone=Asia/Shanghai
    username: root
    password: B6Yp9F9m
    tomcat:
      initSQL: SET NAMES utf8mb4 COLLATE utf8mb4_unicode_ci
  mvc:
    view:
      suffix: .html
      prefix: /

